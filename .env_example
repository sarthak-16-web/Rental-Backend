PORT=5001

MONGODB_URI=mongodb://127.0.0.1:27017/rentalking

JWT_ACCESS_SECRET=change-me-access-secret
CORS_ORIGIN=http://localhost:3000

# Optional — if unset, image uploads fall back to local disk (served from /uploads).
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
